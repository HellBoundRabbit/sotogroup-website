rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all - no authentication required (original setup)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
