rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No rules - completely open (like original setup)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
